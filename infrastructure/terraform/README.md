# Infrastructure as Code (Terraform)

Planned modules:

- `network/` VPC, private subnets, service mesh boundary
- `compute/` Kubernetes cluster and node pools
- `data/` managed PostgreSQL, Redis, OpenSearch, Neo4j connectors
- `security/` KMS, secrets management, audit sinks, policy-as-code
