export const siteConfig = {
  name: "Community Connect",
  description: "Local community engagement, public safety, and services platform",
  url: process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000",
} as const;
